// Tailwind v4 uses CSS-first config via @theme in globals.css
// This file is kept for editor compatibility only
import type { Config } from "tailwindcss";
const config: Config = { content: [] };
export default config;